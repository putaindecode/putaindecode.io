mixin Avatar(author, tooltip)
  -var gravatar = contributors.map[author.login].gravatar_id || "default"
  -var className = ""
  -if (tooltip)
    -className += "AttrTip AttrTip--bottom"
  a(href="#{author.url ? author.url : 'https://github.com/' + author.login}", data-tip="#{author.login}", class="#{attributes.class} #{className}")
    img(
      class="putainde-Avatar js-AnimateLoad"
      src="http://www.gravatar.com/avatar/#{gravatar}.png"
      alt=""
    )
